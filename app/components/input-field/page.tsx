import type { Metadata } from "next"

import { ComponentDoc } from "@/components/prism/component-doc"

export const metadata: Metadata = { title: "Input / Field" }

export default function Page() { return <ComponentDoc slug="input-field" /> }
