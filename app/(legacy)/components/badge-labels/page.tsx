import type { Metadata } from "next"

import { ComponentDoc } from "@/components/prism/component-doc"

export const metadata: Metadata = { title: "Badge & Labels" }

export default function Page() { return <ComponentDoc slug="badge-labels" /> }
