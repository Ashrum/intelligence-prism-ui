import type { Metadata } from "next"
import { ComponentDoc } from "@/components/prism/component-doc"

export const metadata: Metadata = { title: "Checkbox / Radio Group / Switch" }
export default function Page() { return <ComponentDoc slug="choice-controls" /> }
